# VOC Command Center

Next.js 15 dashboard for VOC MVP v0.1.

## Local Development

```bash
npm install
npm run dev
```

Open `http://127.0.0.1:3010`.

## Docker Compose

From `/opt/voc`:

```bash
docker compose up --build
```

The service binds to `127.0.0.1:3010` only.
